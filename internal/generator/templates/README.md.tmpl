# {{ .ProjectName }}

这是一个使用 wb2-cli 工具生成的 Ai-Thinker WB2 项目。

## 项目信息

- **项目名称**: {{ .ProjectName }}
- **SDK 路径**: {{ .SDKPath }}

## 已包含的组件

{{- if .Components }}
{{- range .Components }}
- **{{ .Name }}**: {{ .Description }}
{{- end }}
{{- else }}
- 基础组件（无额外组件）
{{- end }}

## 编译

```bash
make -j8
```

## 烧录

连接开发板后，运行：

```bash
make flash p=/dev/ttyUSB0 b=921600
```

## 配置说明

{{- if .HasWifi }}
### Wi-Fi 配置

请在 `{{ .ProjectName }}/main.c` 中修改以下宏定义：

```c
#define ROUTER_SSID "your_wifi_ssid"
#define ROUTER_PWD "your_wifi_password"
```
{{- end }}

## 项目结构

```
{{ .ProjectName }}/
├── Makefile              # 项目构建文件
├── proj_config.mk        # 项目配置文件
├── README.md             # 本文件
└── {{ .ProjectName }}/   # 项目源代码目录
    ├── main.c            # 主程序
    ├── bouffalo.mk       # 组件构建文件
    └── include/          # 头文件目录
        └── main_board.h  # 主板配置头文件
```

## 开发指南

1. 修改 `{{ .ProjectName }}/main.c` 实现您的业务逻辑
2. 如需添加新的组件，请在 `Makefile` 中的 `INCLUDE_COMPONENTS` 添加
3. 修改 `proj_config.mk` 调整编译选项和功能开关

## 更多信息

请参考 [Ai-Thinker WB2 SDK 文档](https://github.com/Ai-Thinker-Open/Ai-Thinker-WB2)
